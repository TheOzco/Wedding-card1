# 💍 راهنمای استفاده از کارت دعوت دیجیتالی عروسی

## 📋 فهرست مطالب
1. [تغییر اطلاعات شخصی](#تغییر-اطلاعات-شخصی)
2. [دیپلوی روی گیت‌هاب](#دیپلوی-روی-گیتهاب)
3. [تنظیمات پیشرفته](#تنظیمات-پیشرفته)

---

## 🎯 تغییر اطلاعات شخصی

### ۱. تغییر نام عروس و داماد

فایل: `src/components/HomePage.tsx`

خطوط ۵۱-۶۱ را پیدا کنید:

```tsx
<div className="text-center">
  <p className="text-3xl md:text-4xl font-semibold text-pink-600 mb-2">علی</p>
  ...
</div>
...
<div className="text-center">
  <p className="text-3xl md:text-4xl font-semibold text-pink-600 mb-2">زهرا</p>
  ...
</div>
```

**"علی"** و **"زهرا"** را با نام‌های واقعی جایگزین کنید.

---

### ۲. تغییر تاریخ عروسی

فایل: `src/components/HomePage.tsx`

خط ۶:

```tsx
const weddingDate = new Date('2024-12-25T18:00:00');
```

تاریخ را به فرمت `YYYY-MM-DDTHH:MM:SS` تغییر دهید.

همچنین خطوط ۱۱۵-۱۲۴ را برای تاریخ فارسی:

```tsx
<p className="text-gray-700">پنجشنبه، ۴ دی ماه ۱۴۰۳</p>
...
<p className="text-gray-700">ساعت ۱۸:۰۰</p>
```

---

### ۳. تغییر آدرس و لوکیشن تالار

فایل: `src/components/DetailsPage.tsx`

خطوط ۱۱-۱۵:

```tsx
const venueLocation = {
  name: 'تالار پذیرایی کوثر',
  address: 'تهران، خیابان ولیعصر، نرسیده به میدان ونک، پلاک ۱۲۳',
  lat: 35.7219,
  lng: 51.4056,
};
```

**نام تالار**، **آدرس** و **مختصات جغرافیایی (lat, lng)** را تغییر دهید.

#### 🗺️ چگونه مختصات جغرافیایی پیدا کنیم؟

1. به [Google Maps](https://maps.google.com) بروید
2. مکان تالار را پیدا کنید
3. روی مکان کلیک راست کنید
4. روی مختصات (اعداد) کلیک کنید تا کپی شود
5. عدد اول = lat، عدد دوم = lng

---

### ۴. ویرایش داستان عشق

فایل: `src/components/StoryPage.tsx`

خطوط ۱۱-۴۱ (آرایه timeline):

```tsx
const timeline = [
  {
    year: '۱۳۹۸',
    title: 'اولین دیدار',
    description: 'روزی که برای اولین بار همدیگر را دیدیم و قلب‌هایمان تند زد...',
    emoji: '👀',
  },
  // موارد دیگر...
];
```

هر آیتم را با داستان واقعی خودتان جایگزین کنید.

---

### ۵. تغییر اطلاعات تماس

فایل: `src/components/RSVPPage.tsx`

خط ۱۷۲:

```tsx
<p className="text-gray-600 text-center" dir="ltr">
  ۰۹۱۲-۳۴۵-۶۷۸۹
</p>
```

شماره تماس خود را وارد کنید.

خط ۱۸۲:

```tsx
<p className="text-gray-600 text-center">
  تا ۲۰ آذر ماه
</p>
```

مهلت تأیید حضور را تغییر دهید.

---

### ۶. تغییر هشتگ و آی‌دی اینستاگرام

فایل: `src/components/GalleryPage.tsx`

خطوط ۱۰۴-۱۱۳:

```tsx
<p className="text-gray-700">
  <span className="font-semibold text-rose-500">هشتگ:</span> #علی_زهرا_۱۴۰۳
</p>
...
<p className="text-gray-700">
  <span className="font-semibold text-rose-500">اینستاگرام:</span> @ali_zahra_wedding
</p>
```

---

## 🚀 دیپلوی روی گیت‌هاب

### مرحله ۱: ایجاد مخزن در گیت‌هاب

1. به [GitHub.com](https://github.com) بروید
2. روی دکمه **"New"** یا **"+"** در بالای صفحه کلیک کنید
3. نام مخزن را وارد کنید (مثلاً: `wedding-invitation`)
4. روی **"Create repository"** کلیک کنید

---

### مرحله ۲: آپلود کدها

در ترمینال (Terminal) یا Command Prompt:

```bash
# وارد پوشه پروژه شوید
cd path/to/your/project

# Git را راه‌اندازی کنید
git init

# فایل‌ها را اضافه کنید
git add .

# کامیت اول
git commit -m "Initial commit: Wedding invitation card"

# برنچ اصلی را تنظیم کنید
git branch -M main

# آدرس مخزن گیت‌هاب را اضافه کنید (آدرس خودتان را جایگزین کنید)
git remote add origin https://github.com/YOUR_USERNAME/wedding-invitation.git

# کدها را آپلود کنید
git push -u origin main
```

---

### مرحله ۳: فعال‌سازی GitHub Pages

1. به مخزن گیت‌هاب خود بروید
2. **Settings** → **Pages** (از منوی سمت چپ)
3. در قسمت **Source**، گزینه **"GitHub Actions"** را انتخاب کنید
4. صبر کنید تا دیپلوی اتوماتیک انجام شود (۲-۳ دقیقه)
5. لینک سایت شما نمایش داده می‌شود: `https://YOUR_USERNAME.github.io/wedding-invitation/`

---

### مرحله ۴: بروزرسانی

هر بار که تغییری دادید:

```bash
git add .
git commit -m "Update wedding details"
git push
```

سایت به طور خودکار بروز می‌شود! ✨

---

## 🎨 تنظیمات پیشرفته

### تغییر رنگ‌های اصلی

فایل: `tailwind.config.js` (اگر وجود ندارد، ایجاد کنید)

```js
export default {
  theme: {
    extend: {
      colors: {
        primary: '#ec4899', // رنگ اصلی
        secondary: '#f43f5e', // رنگ ثانویه
      }
    }
  }
}
```

---

### تغییر فونت

فایل: `index.html`

خط ۷-۹:

```html
<link href="https://fonts.googleapis.com/css2?family=FONT_NAME&display=swap" rel="stylesheet">
```

فونت دلخواه خود را از [Google Fonts](https://fonts.google.com) انتخاب کنید.

---

## 🆘 رفع مشکلات رایج

### ۱. سایت نمایش داده نمی‌شود

- چند دقیقه صبر کنید
- بررسی کنید که GitHub Actions با موفقیت اجرا شده باشد (تب Actions در گیت‌هاب)
- مطمئن شوید Settings → Pages فعال است

### ۲. تغییرات نمایش داده نمی‌شود

- Cache مرورگر را پاک کنید (Ctrl+F5 یا Cmd+Shift+R)
- چند دقیقه صبر کنید تا دیپلوی کامل شود

### ۳. خطا در بیلد

- `npm install` را مجدد اجرا کنید
- مطمئن شوید Node.js نسخه ۱۸ یا بالاتر دارید

---

## 📱 تست روی موبایل

برای تست در موبایل:

1. روی موبایل خود به آدرس سایت بروید
2. یا در مرورگر دسکتاپ، Developer Tools را باز کنید (F12)
3. روی آیکون موبایل کلیک کنید
4. اندازه‌های مختلف را تست کنید

---

## 💝 نکات مهم

- ✅ همه اطلاعات شخصی را **قبل از دیپلوی** تغییر دهید
- ✅ مختصات جغرافیایی را دقیق وارد کنید
- ✅ شماره تماس و تاریخ‌ها را چک کنید
- ✅ سایت را روی موبایل و دسکتاپ تست کنید
- ✅ لینک سایت را با دوستان تست کنید قبل از ارسال عمومی

---

## 🎉 موفق باشید!

امیدواریم این کارت دعوت، مراسم زیبایی برای شما رقم بزند! 💕

در صورت نیاز به کمک، می‌توانید Issue در گیت‌هاب ایجاد کنید.
